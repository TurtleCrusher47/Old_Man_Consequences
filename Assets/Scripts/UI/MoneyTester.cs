using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class MoneyTester : MonoBehaviour
{
    // public MoneyAnimation moneyAnimation; // Reference to the MoneyAnimation script

    // private void Update()
    // {
    //     if (Input.GetKeyDown(KeyCode.UpArrow))
    //     {
    //         int randomIndex = Random.Range(0, moneyAnimation.moneyTexts.Length);
    //         int currentValue = int.Parse(moneyAnimation.moneyTexts[randomIndex].text);
    //         int increaseAmount = Random.Range(1, 100); // Replace this with your source for increasing amount
    //         int targetValue = currentValue + increaseAmount;
    //         moneyAnimation.AnimateMoneyChange(randomIndex, currentValue, targetValue);
    //     }

    //     if (Input.GetKeyDown(KeyCode.DownArrow))
    //     {
    //         int randomIndex = Random.Range(0, moneyAnimation.moneyTexts.Length);
    //         int currentValue = int.Parse(moneyAnimation.moneyTexts[randomIndex].text);
    //         int decreaseAmount = Random.Range(1, 100); // Replace this with your source for decreasing amount
    //         int targetValue = Mathf.Max(currentValue - decreaseAmount, 0);
    //         moneyAnimation.AnimateMoneyChange(randomIndex, currentValue, targetValue);
    //     }
    // }
}
